<% if (productos.length > 0) { %> <% productos.forEach(element => { %>
  <table>
    <tr>
        <th>Nombre</th>
        <th>Price</th>
        <th>Thumbnail</th>
    </tr>
    <% productos.forEach(p=>{%>
        <tr>
            <td><%= element.first_name %></td>
            <td><%= element.last_name %></td>
            <td><%=element.age %></td>
        </tr>
    <%}) %>
  </table>
<% })} else { %> No hay nada cargado <% } %>


